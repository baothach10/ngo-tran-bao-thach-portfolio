export type TImageResults = {
  type: string;
  buffer: ArrayBuffer;
};

export type THdriResults = {
  type: string;
  buffer: ArrayBuffer;
};

export type TSvgIconProps = {
  width?: number;
  height?: number;
  className?: string;
};
